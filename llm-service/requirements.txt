fastapi==0.141.1
uvicorn[standard]==0.30.6
# starlette is a transitive dep of fastapi, but is pinned explicitly here because fastapi==0.115.0's
# own version cap (<0.39.0) silently resolved to starlette 0.38.6, which OSV (osv.dev) lists as
# affected by 7 distinct advisories (3 HIGH, incl. CVE-2024-47874 DoS and several request.url
# Host/path spoofing issues fixed only in 1.x) — not the 12 an earlier revision of this comment
# claimed, which appears to have double-counted duplicate GHSA/CVE aliases for the same underlying
# advisories rather than reflecting 12 distinct issues. The earliest starlette release that closes
# all 7 is 1.3.1; 1.6.0 is pinned below as the latest available at fix time rather than the minimum.
# fastapi had to be bumped too since no 0.115.x release lifts that cap far enough — the cap was only
# lifted around fastapi 0.133 (verified locally by checking each release's dependency metadata).
starlette==1.6.0
anthropic==1.0.0
pydantic>=2.0.0,<3.0.0
