# 閉域モード(Closed Mode)実測精度

> **注記**: 本文書は`closed-mode`ブランチの実装についての実測値をまとめたものです。`docs/public/`配下の他の文書(`architecture.md`等)は非閉域モード(通常)ビルドを前提としているため、精度に関する記述は本文書を正としてください。

最終更新: 2026-09-06

## 1. 概要

このドキュメントは、外部ネットワーク通信を一切行わない「閉域モード」ビルドで実際に測定した識別精度をまとめたものです。

- **測定方法**: golden-300ベンチマーク(実在の製品・実在のバージョンで構成した300件の固定データセット。内訳はパッケージレジストリ経由で識別できる200件、パッケージレジストリを持たない製品をCPE辞書照合で識別する68件、いずれの経路でも識別されないことが正解の対照行32件)を、実際にビルドした閉域モードのバックエンドから、パッケージレジストリ9種類のローカルミラーを投入済みの実データベースに対して実行した。
- 閉域モードにはAI(Claude API)を呼び出す経路が物理的に存在しない(コード自体が削除されている)ため、この測定は常に静的パイプラインのみで完結し、**AIコストは常に$0**。
- 以下の数値はいずれも、実際にビルド・実行して得られた実測値であり、投影値やシミュレーション値ではない。

## 2. 全体の識別精度

| 指標 | 実測値 |
| --- | --- |
| 識別対象recall | 247/268 (92.16%) |
| 対照行(本来識別されるべきでない行)の誤検知率 | 1/32 (3.13%) |

これは、npm・PyPI・RubyGems・crates.io・Packagist・NuGet・Hex・pub.dev・Goの9エコシステムをローカルミラーとして投入した状態での実測値。Maven Centralのみ、後述の理由により意図的にミラーを持たない。

## 3. CPE辞書照合の精度

| 指標 | 実測値 |
| --- | --- |
| CPE `vendor:product` 完全一致率 | 62/68 (91.18%) |

パッケージレジストリを持たない製品(OS・デスクトップアプリケーション・ネットワーク機器等)68件について、識別結果のCPE識別子(vendor:product)が正解データと完全に一致するかを検証した。「識別はできたが、期待したのとは違う製品として識別された」というケースも不一致として扱う、識別可否だけを見るよりも厳しい基準の指標。不一致6件の傾向は「6. 既知の残存ギャップ」を参照。

## 4. Maven Centralについて(意図的な既知の制約であり、不具合ではない)

Maven CentralはToS(利用規約)上の理由から、閉域モードでは意図的にローカルミラーを持たない設計としている。Maven座標(`groupId:artifactId`形式)で表現される行は、レジストリ照合が原理的に成立しない。また、これらの行の表記(例: `org.springframework:spring-core`)は人間が読む製品名とは形式が異なるため、CPE辞書側の照合(製品名の表記ゆれ吸収)によって偶然救済されることもない。

**実測**: golden-300のうちMaven座標を持つ20件は、全件が識別不能(0/20)。これは設計上意図された既知の制約であり、バグではない。

## 5. マーケットプレイス拡張系(ブラウザ拡張・VS Code拡張・JetBrainsプラグイン等)の精度

現時点の閉域モードビルドには、インストールURLのホスト名等からブラウザ拡張・VS Code拡張・JetBrainsプラグインといった「マーケットプレイス拡張系」製品を専用に判定するロジックがまだ実装されておらず、通常のCPE辞書照合と同じ経路を通る。この専用ロジックを前提とした精度測定はまだ実施できていないため、本資料では数値を記載しない。実装・計測が完了し次第、本文書に追記する。

## 6. 既知の残存ギャップ

いずれも一般的な技術的制約であり、個別の修正状況はここでは扱わない。

- **vendor名の表記ゆれ**: 一部の製品では、CPE辞書上のvendor名が一般に知られている会社名・製品名の通称と異なることがある(例: 買収・組織再編前後で正式名称が変わった、業界で通称と正式ベンダー名が食い違う等)。この場合、製品自体の識別には成功しても、期待したvendor:product識別子とは一致しないことがある。
- **派生製品・バンドル版との混同**: 同一ベンダーが本体製品と非常によく似た名前の派生製品・バンドル版を別のCPE識別子としてカタログ化しているケースでは、検索条件が本体・派生版のどちらにも一致してしまい、本来の製品ではなく近縁の派生製品として識別されることがある。
- **辞書側の欠落・類似度不足**: 一部の製品は、CPE辞書側に該当エントリ自体が存在しない、またはあいまい一致の類似度が閾値に届かないために、識別不能のままとなる。

## 7. 測定手法についての注記

golden-300データセットは、各行の用途説明文にエコシステム名(パッケージレジストリ名等)が明示的に含まれる作りになっており、実際の利用シーンで入力される自由記述のCSVよりも識別しやすい傾向がある。別途、エコシステム名のヒントを含まない、より実データに近い1,000件規模のデータセットでクロス検証した結果、識別率はgolden-300単独の数値よりやや低めになることを確認している。上記の数値は実力の上限に近い、やや楽観的な値である可能性がある点に留意されたい。
